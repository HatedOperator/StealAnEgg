# API + bot
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.6
discord.py>=2.3

# OCR (needs the Tesseract binary installed separately — see README)
Pillow>=10.2
pytesseract>=0.3.10

# Scanner (screen capture + window/process discovery)
mss>=9.0
pygetwindow>=0.0.9
psutil>=5.9
